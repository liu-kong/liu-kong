# .github/workflows/stats.yml
name: Update Stats
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: liu-kong/github-readme-stats@latest
        with:
          COMMIT_MSG: "📊 Auto Update Metrics"